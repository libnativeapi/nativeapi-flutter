name: browser_tabs_example
description: "Chrome-style tabs: reorder, tear off into new windows, and merge back while dragging."
publish_to: "none"
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  nativeapi:
    path: ../../bindings/dart/nativeapi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
