name: drag_drop_example
description: "A custom title bar and resize handles built with DragToMoveArea and DragToResizeArea."
publish_to: "none"
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  nativeapi_flutter:
    path: ../../bindings/dart/nativeapi_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
