name: detachable_window_example
description: "Tear a panel out of the main window into its own window and dock it back, keeping its Flutter state."
publish_to: "none"
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  nativeapi:
    path: ../../bindings/flutter/nativeapi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
