name: shaped_window_example
description: "Native polygon windows: circle, star, and speech bubble."
publish_to: "none"
version: 1.0.0+1
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  nativeapi:
    path: ../../bindings/flutter/packages/nativeapi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
