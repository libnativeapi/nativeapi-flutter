// A workbench of native views — labels, buttons, text fields and an image view
// in rows and columns — from a plain Dart program: `dart run`, no Flutter.

import 'package:nativeapi/nativeapi.dart';
import 'package:view_example/view_example.dart';

void main() => runNativeApp(app);
